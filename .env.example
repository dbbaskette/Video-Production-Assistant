# ─── VPA Application ───────────────────────────────────────────────
# Where VPA stores app config (tracker, brands, voices)
VPA_HOME=~/.vpa

# Default folder root for new projects
VPA_PROJECTS_DEFAULT=~/Movies/VPA

# ─── Server ────────────────────────────────────────────────────────
VPA_SERVER_PORT=3000
VPA_SERVER_HOST=127.0.0.1

# ─── Web (Vite reads VITE_* prefix) ───────────────────────────────
VITE_VPA_API_BASE=http://localhost:3000

# ─── LLM Provider ─────────────────────────────────────────────────
# Options: fake | claude-code | gemini | anthropic
#
#   fake         — deterministic dev responses, no external calls (default)
#   claude-code  — uses `claude -p` subprocess, requires Claude Code CLI installed
#                  and logged in. No API key needed — uses your Claude subscription.
#   gemini       — Google Gemini REST API, requires GEMINI_API_KEY
#   anthropic    — Anthropic REST API, requires ANTHROPIC_API_KEY
#
VPA_LLM_PROVIDER=fake

# ─── Per-provider model overrides ─────────────────────────────────
# Each provider reads its own env var first, then falls back to VPA_LLM_MODEL.
#
# CLAUDE_MODEL=sonnet                       # default: sonnet
# GEMINI_MODEL=gemini-2.5-flash-lite        # default: gemini-2.5-flash-lite
# ANTHROPIC_MODEL=claude-sonnet-4-20250514  # default: claude-sonnet-4-20250514
# VPA_LLM_MODEL=                            # generic fallback for any provider

# ─── API Keys ─────────────────────────────────────────────────────
# Claude Code CLI — no API key needed, just install Claude Code and log in:
#   https://claude.ai/code

# Gemini (Google AI) — required when VPA_LLM_PROVIDER=gemini
# Get a key at https://aistudio.google.com/apikey
# GEMINI_API_KEY=

# Anthropic REST API — required when VPA_LLM_PROVIDER=anthropic
# Get a key at https://console.anthropic.com/settings/keys
# ANTHROPIC_API_KEY=

# ─── TTS API Keys ────────────────────────────────────────────────
# Gemini TTS uses the same GEMINI_API_KEY above (Google AI)
#
# xAI (Grok) TTS — get a key at https://console.x.ai
# Five voices: Eve, Ara, Leo, Rex, Sal
# XAI_API_KEY=

# ─── Fish Audio (local, Apple Silicon — mlx_audio) ──────────────
# No API key needed. Requires Python 3 + mlx_audio:
#   pip install mlx_audio
# Model auto-detected from LM Studio's standard path.
#
# FISH_AUDIO_MODEL=~/.lmstudio/models/mlx-community/fish-audio-s2-pro-bf16
# FISH_AUDIO_STYLE=professional broadcast tone
# FISH_AUDIO_SPEED=1.0
# FISH_AUDIO_EXAGGERATION=0.3
# FISH_AUDIO_TEMPERATURE=0.7
# FISH_AUDIO_REF_AUDIO=path/to/reference/sample.wav
# FISH_AUDIO_REF_TEXT="Exact transcript of the reference audio"
